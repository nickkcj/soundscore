version: "3.13" 

services:
  web:
    build: .
    volumes:
      - .:/soundscore
      - ./media:/soundscore/media
      - ./db.sqlite3:/soundscore/db.sqlite3
    ports:
      - "8000:8000"
    environment:
      - DEBUG=True
    depends_on:
      - redis  # 🟢 Adiciona dependência do Redis
    command: >
      sh -c "python manage.py migrate &&
             python manage.py runserver 0.0.0.0:8000"

  redis:
    image: redis:alpine
    ports:
      - "6379:6379"  # Expor não é obrigatório, mas pode ajudar para debug
